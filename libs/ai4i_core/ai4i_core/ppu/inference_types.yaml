inference_types:
  - name: llm
    endpoint_pattern: /api/v1/chat
    unit: tokens
    pricing: per_million_tokens
  - name: asr
    endpoint_pattern: /api/v1/asr/inference
    unit: audio_minutes
    pricing: per_minute
  - name: nmt
    endpoint_pattern: /api/v1/nmt/inference
    unit: characters
    pricing: per_million_characters
  - name: tts
    endpoint_pattern: /api/v1/tts/inference
    unit: characters
    pricing: per_million_characters
  - name: ner
    endpoint_pattern: /api/v1/ner/inference
    unit: characters
    pricing: per_million_characters
  - name: ocr
    endpoint_pattern: /api/v1/ocr/inference
    unit: images
    pricing: per_image
  - name: transliteration
    endpoint_pattern: /api/v1/transliteration/inference
    unit: characters
    pricing: per_million_characters
  - name: language-detection
    endpoint_pattern: /api/v1/language-detection/inference
    unit: characters
    pricing: per_million_characters
  - name: language-diarization
    endpoint_pattern: /api/v1/language-diarization/inference
    unit: audio_minutes
    pricing: per_minute
  - name: speaker-diarization
    endpoint_pattern: /api/v1/speaker-diarization/inference
    unit: audio_minutes
    pricing: per_minute
  - name: audio-lang-detection
    endpoint_pattern: /api/v1/audio-lang-detection/inference
    unit: audio_minutes
    pricing: per_minute
  - name: pipeline
    endpoint_pattern: /api/v1/pipeline/inference
    unit: requests
    pricing: per_request
