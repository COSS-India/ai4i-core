# Direct dependencies pinned to build-verified versions (12-Factor V:
# deterministic builds — the same Dockerfile must produce the same image).
# Versions captured via `pip freeze` from the Dockerfile builder stage.
# To upgrade: bump the pin, rebuild, run the test suite.

# ── Shared platform library (consolidated) ──
ai4i-core==1.0.5.1

# FastAPI and ASGI server
fastapi==0.136.3
uvicorn[standard]==0.49.0

# Data validation
pydantic==2.13.4
pydantic-settings==2.14.1

# HTTP client
httpx==0.28.1

# Utilities
python-dotenv==1.2.2

# Audio processing
scipy==1.17.1
numpy==2.4.6
soundfile==0.13.1

# JSON Mapping and Conversion

python-multipart>=0.0.6
kafka-python==2.3.2
pydub==0.25.1

# OpenTelemetry tracing (required by trace/setup.py).
# The OTLP/gRPC exporter is NOT pulled transitively by ai4i-core, so without it
# setup_tracing() raises ImportError, disabling the Kafka span exporter and
# stopping PPU billing. api/sdk pinned to the same version as the exporter to
# keep them consistent. Confirm against a builder `pip freeze` before release.
opentelemetry-api==1.42.1
opentelemetry-sdk==1.42.1
opentelemetry-exporter-otlp-proto-grpc==1.42.1
grpcio==1.80.0

# Security: pin transitive dependencies to patched versions
idna>=3.15
starlette>=1.0.1
