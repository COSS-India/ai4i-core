# ── Role IDs (must match the seeded values in roles table) ───────────
# Source: services/auth-service/app/core/config.py
class RoleId:
    ADMIN = 1
    MODERATOR = 2
    GUEST = 3
    USER = 4
    TENANT_ADMIN = 5
