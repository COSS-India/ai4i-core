# ── Shared platform library (consolidated) ──
ai4icore-core>=1.1.9

# ── Core framework ──
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# ── Database ──
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
alembic>=1.13.0

# ── Redis ──
redis>=5.0.0

# ── HTTP client (endpoint validation probing + alert-sync reloads) ──
httpx>=0.25.0

# ── Metrics (policy evaluation) ──
prometheus-client>=0.19.0

# ── YAML rendering (alert-sync Prometheus/Alertmanager config files) ──
pyyaml>=6.0

# ── Async file I/O ──
aiofiles>=23.0.0

# ── Observability (optional) ──
opentelemetry-api>=1.20.0
opentelemetry-sdk>=1.20.0
opentelemetry-exporter-otlp-proto-grpc>=1.20.0
opentelemetry-instrumentation-fastapi>=0.41b0
opentelemetry-instrumentation-sqlalchemy>=0.41b0
opentelemetry-instrumentation-redis>=0.41b0

# ── OpenSearch client ──
opensearch-py>=2.0.0

# ── Testing ──
pytest>=7.4.0
pytest-asyncio>=0.21.0

# ── Security: pin transitive dependencies to patched versions ──
mako>=1.3.12
idna>=3.15
starlette>=1.0.1
