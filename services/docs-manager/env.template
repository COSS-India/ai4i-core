# Docs Manager – environment template
# Copy to .env and adjust as needed. All variables are optional (defaults work for local dev).

# Base URL of the API Gateway. Used by /api-proxy when USE_API_PROXY=true, and as the
# OpenAPI server URL when USE_API_PROXY=false (deployed behind APISIX, etc.).
# Default: http://localhost:8080
# Local:  API_GATEWAY_URL=http://localhost:8080
# Docker: API_GATEWAY_URL=http://api-gateway-service:8080
# Deploy: API_GATEWAY_URL=https://dev.ai4inclusion.org
API_GATEWAY_URL=http://api-gateway-service:8080

# Use same-origin proxy for "Try it out" (true) or call API_GATEWAY_URL directly (false).
# Set false when deployed behind a gateway that has no /api-proxy route (e.g. APISIX).
# Default: true
USE_API_PROXY=true

# Absolute path to the service docs registry YAML file. The registry lists which
# services to include and their spec_file paths.
# Default: <service_dir>/specs/service-docs-registry.yaml
# SERVICE_DOCS_REGISTRY_PATH=/app/specs/service-docs-registry.yaml

# Root directory for resolving spec_file paths from the registry (e.g. asr-service.yaml).
# Default: <service_dir>/specs
# SPECS_ROOT=/app/specs
