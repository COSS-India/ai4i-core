
# ── Shared platform library (consolidated) ──
ai4i-core==1.0.27

# Pinned: the consumer design depends on version-specific behaviour —
# cooperative-sticky, the batch API, store_offsets, incremental_assign and
# commit()'s _NO_OFFSET semantics (ARCHITECTURE.md §6).
confluent-kafka==2.15.0

# ── Kafka (notification producer side-channel — payperuse_consumer only) ──
kafka-python==2.3.2

sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
httpx>=0.25.0
pydantic-settings>=2.1.0
# jinja2 / aiosmtplib come in transitively via ai4i-core (its email module —
# see emailer.py); not re-pinned here, same as auth-service/platform-core-service.
cryptography>=41.0.0

# ── Testing ──
pytest>=7.4.0
pytest-asyncio>=0.21.0
