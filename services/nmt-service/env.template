# NMT Service Environment Configuration Template
# Copy this file to .env and update the values
#
# NOTE: Most configurations are now managed via Config Service.
# Only essential variables for connecting to Config Service and Service Registry are kept here.
# See manage-configs.sh for creating configurations in Config Service.

# Service Registry Configuration (Required for service registration)
SERVICE_NAME=nmt-service
SERVICE_PORT=8089
SERVICE_HOST=nmt-service
SERVICE_PUBLIC_URL=
SERVICE_INSTANCE_ID=

# Config Service Connection (Required to fetch configurations)
CONFIG_SERVICE_URL=http://config-service:8082
ENVIRONMENT=development

# Logging (Optional - can also be configured in Config Service)
LOG_LEVEL=INFO

# ============================================================================
# The following configurations should be created in Config Service:
# - REDIS_HOST, REDIS_PORT, REDIS_PASSWORD
# - DATABASE_URL
# - TRITON_ENDPOINT, TRITON_API_KEY
# - RATE_LIMIT_PER_MINUTE, RATE_LIMIT_PER_HOUR
# - MAX_BATCH_SIZE, MAX_TEXT_LENGTH
# - DEFAULT_SOURCE_LANGUAGE, DEFAULT_TARGET_LANGUAGE
#
# Use the setup-configs.sh script or see CONFIG_SETUP.md for curl commands
# ============================================================================
