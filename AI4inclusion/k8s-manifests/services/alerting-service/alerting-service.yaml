apiVersion: v1
kind: Service
metadata:
  name: alerting-service
  namespace: dev
  labels:
    app: alerting-service
spec:
  selector:
    app: alerting-service
  ports:
    - port: 8085
      targetPort: 8085
      protocol: TCP
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: alerting-service
  namespace: dev
  labels:
    app: alerting-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: alerting-service
  template:
    metadata:
      labels:
        app: alerting-service
    spec:
      nodeSelector:
      tolerations:
      - key: "node-type"
        operator: Equal
        value: cpu
        effect: NoSchedule
      containers:
      - name: alerting-service
        image: ai4v/alerting-service:latest
        ports:
        - containerPort: 8085
        env:
        - name: ENV_FILE
          value: ".env"
        envFrom:
        - configMapRef:
            name: alerting-service-config
        - secretRef:
            name: alerting-service-secret
        resources:
          requests:
            memory: "256Mi"
            cpu: "200m"
          limits:
            memory: "512Mi"
            cpu: "400m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8085
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /ready
            port: 8085
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
      restartPolicy: Always
