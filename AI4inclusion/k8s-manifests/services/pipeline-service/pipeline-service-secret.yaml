apiVersion: v1
kind: Secret
metadata:
  name: pipeline-secrets
  namespace: dev
type: Opaque
stringData:
  # Database Configuration
  DATABASE_URL: "postgresql+asyncpg://ai4v_user:password123@postgres:5432/ai4v_db" 
  
  # Redis Configuration - must match redis-secret
  REDIS_HOST: "redis.dev.svc.cluster.local"
  REDIS_PORT: "6379"
  REDIS_PASSWORD: "redispass"