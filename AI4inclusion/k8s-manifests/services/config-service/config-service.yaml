apiVersion: v1
kind: Service
metadata:
  name: config-service
  namespace: dev
  labels:
    app: config-service
spec:
  selector:
    app: config-service
  ports:
    - port: 8082
      targetPort: 8082
      protocol: TCP
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: config-service
  namespace: dev
  labels:
    app: config-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: config-service
  template:
    metadata:
      labels:
        app: config-service
    spec:
      nodeSelector:
      tolerations:
      - key: "node-type"
        operator: Equal
        value: cpu
        effect: NoSchedule
      containers:
      - name: config-service
        image: 662074586476.dkr.ecr.ap-south-1.amazonaws.com/ai4voice/config-service:27112025-125033-IST
        ports:
        - containerPort: 8082
        env:
        - name: ENV_FILE
          value: ".env"
        envFrom:
        - configMapRef:
            name: config-service-config
        - secretRef:
            name: config-service-secret
        resources:
          requests:
            memory: "256Mi"
            cpu: "200m"
          limits:
            memory: "512Mi"
            cpu: "400m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8082
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /ready
            port: 8082
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
      restartPolicy: Always
