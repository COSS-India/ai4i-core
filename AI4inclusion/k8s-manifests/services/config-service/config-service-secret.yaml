apiVersion: v1
kind: Secret
metadata:
  name: config-service-secret
  namespace: dev
type: Opaque
stringData:
  # PostgreSQL Configuration
  DATABASE_URL: "postgresql+asyncpg://ai4v_user:password123@postgres.dev.svc.cluster.local:5432/config_db"

  # Redis Configuration
  REDIS_PASSWORD: "redispass"

  # Vault Configuration
  VAULT_TOKEN: "your_vault_token_here"

  # Unleash Feature Flag Configuration
  UNLEASH_API_TOKEN: "*:*.unleash-insecure-api-token"
