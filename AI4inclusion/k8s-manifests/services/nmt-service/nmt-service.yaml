apiVersion: v1
kind: Service
metadata:
  name: nmt-service
  namespace: dev
  labels:
    app: nmt-service
spec:
  selector:
    app: nmt-service
  ports:
    - port: 8089
      targetPort: 8089
      protocol: TCP
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nmt-service
  namespace: dev
  labels:
    app: nmt-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nmt-service
  template:
    metadata:
      labels:
        app: nmt-service
    spec:
      nodeSelector:
      tolerations:
      - key: "node-type"
        operator: Equal
        value: cpu
        effect: NoSchedule
      containers:
      - name: nmt-service
        image: 662074586476.dkr.ecr.ap-south-1.amazonaws.com/ai4voice/nmt-service:v1.4
        ports:
        - containerPort: 8089
        env:
        - name: ENV_FILE
          value: ".env"
        envFrom:
        - configMapRef:
            name: nmt-service-config
        - secretRef:
            name: nmt-service-secret
        resources:
          requests:
            memory: "512Mi"
            cpu: "300m"
          limits:
            memory: "1Gi"
            cpu: "600m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8089
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8089
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
      restartPolicy: Always
