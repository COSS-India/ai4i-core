# Migration Framework Requirements
# Install with: pip install -r requirements.txt

# PostgreSQL
sqlalchemy>=2.0.0
asyncpg>=0.29.0
psycopg2-binary>=2.9.0

# Redis
redis>=5.0.0

# Elasticsearch
elasticsearch>=8.10.0

# Kafka
kafka-python>=2.0.2

# Core dependencies
python-dotenv>=1.0.0

# Pydantic (needed for loading service models during migrations)
pydantic[email]>=2.4.0
pydantic-settings>=2.0.0

# Alembic for database migrations
alembic>=1.12.0

# Password hashing (used in seeders like auth default admin)
bcrypt>=4.0.0
passlib[argon2]>=1.7.4
argon2-cffi>=25.1.0

# PII field encryption (AES-SIV) — used by the auth-service models loaded during
# migrations and by the encrypt-PII data migration to encrypt existing rows.
cryptography>=41.0.0

# Security: pin transitive dependencies to patched versions
mako>=1.3.12
idna>=3.15
starlette>=1.0.1
