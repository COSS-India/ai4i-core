# 3. Create an API Key

| | |
| --- | --- |
| Objective | Create the API key an application uses to authenticate its requests to a service. |
| Role | Tenant Admin |
| Prerequisites | Your institution has an active tier and budget assigned; you know the permissions and expiry the key requires. |

**Step 1** Click "API Key Management" to create a key for your application.

<!-- TODO: add screenshot to assets/create-api-key-01-management-area.png -->
![The API Key Management area](../assets/create-api-key-01-management-area.png)

**Step 2** The "Create API Key" tab opens by default.

<!-- TODO: add screenshot to assets/create-api-key-02-tab-default.png -->
![The Create API Key tab, ready for a new key](../assets/create-api-key-02-tab-default.png)

**Step 3** Enter the required information — Key Name, Permissions, and Expiry — and click "Create API Key."

<!-- TODO: add screenshot to assets/create-api-key-03-fill-form.png -->
![Entering the key name, permissions, and expiry, and clicking Create API Key](../assets/create-api-key-03-fill-form.png)

{% hint style="info" %}
**NOTE**

An API key can be created only if your institution has an active tier assigned. If no tier is assigned, contact your Adopter Admin before proceeding.
{% endhint %}

**Step 4** Once the API key is created, copy it and store it securely — it is shown only once.

{% hint style="danger" %}
**IMPORTANT**

The full API key is displayed only at the moment of creation and cannot be retrieved again afterwards. Copy it and store it securely before leaving this screen. If it is lost, you will need to create a new key.
{% endhint %}

<!-- TODO: add screenshot to assets/create-api-key-04-key-created.png -->
![The API key created, shown once with the option to copy it](../assets/create-api-key-04-key-created.png)

| | |
| --- | --- |
| Outcome | The application has an API key, scoped to the permissions you set and valid until its expiry, ready to authenticate its requests to a service. |
| Next | Proceed to [Part III](../operate-and-govern/metering-dashboard.md) to track your institution's usage and spend. |
